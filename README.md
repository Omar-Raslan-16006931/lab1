# Todo App - Assignment 1

Completed the filter and search assignment. Summary of changes:

- **Backend**: Updated `getTodos` to filter by the `done` query parameter.
- **Frontend**: Updated `fetchTodos` to pass the filter state to the API.
- **UI**: Added All, Active, and Done buttons in `App.jsx` to switch between views, along with basic styling.

Filtering is handled on the server side.
